// ENVIRONMENT CONFIGURATION
// Keys are stored server-side in Vercel environment variables.
// The frontend uses /api/data proxy instead of calling Supabase directly.
window.ECELL_ENV = {
    API_BASE: '/api/data'
};
